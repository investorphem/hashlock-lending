[project]
name = "hashlock-lending"
# clarity_version = "4" # Removed this line (optional but cleaner)
description = ""
authors = []
telemetry = true
requirements = []
analysis = ["check_checker"]
costs_version = 2

[contracts.hashlock-core]
path = "contracts/hashlock-core.clar"
clarity_version = "4"
# Specify local dependencies so they deploy in the correct order
depends_on = [
    "hashlock-isolated-sbtc-v1",
    "safe-vault-base"
]

[contracts.hashlock-isolated-sbtc-v1]
path = "contracts/hashlock-isolated-sbtc-v1.clar"
clarity_version = "4"
depends_on = []

[contracts.safe-vault-base]
path = "contracts/safe-vault-base.clar"
clarity_version = "4"
depends_on = []
